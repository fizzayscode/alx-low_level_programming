#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: 'using printf to print a string to console'
 *
 * Return: always return (0)
 */
 int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
